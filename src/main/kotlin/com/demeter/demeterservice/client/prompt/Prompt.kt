package com.demeter.demeterservice.client.prompt

import com.demeter.demeterservice.client.message.Message

class Prompt(resolvedTemplate: String) {
    private val messages: List<Message>? = null // constructors and utility methods
}